<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | The Jersey Lab ADMIN</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/admin-style.css">
</head>
<body>

    <div class="dashboard-container">
        <header class="admin-header">
            <div class="logo-login" style="font-size: 1.5rem; margin:0; text-align:left;">The Jersey Lab <span>PANEL</span></div>
            <nav class="admin-nav">
                <span>Hola, Admin</span>
                <a href="/admin/ventas/excel"><i class="fas fa-file-excel"></i> Reporte</a>
                <a href="/admin/nuevo_admin"><i class="fas fa-user-plus">Crear administrador</i></a>
                <a href="/admin/logout" style="color: var(--danger);"><i class="fas fa-sign-out-alt"></i> Salir</a>
            </nav>
        </header>

        <div class="stats-grid">
            <div class="stat-card">
                <span class="stat-number"><%= productos.length %></span>
                <span class="stat-label">Total</span>
            </div>
            <div class="stat-card">
                <span class="stat-number" style="color: var(--success);"><%= productos.filter(p=>p.activo).length %></span>
                <span class="stat-label">Activos</span>
            </div>
            <div class="stat-card">
                <span class="stat-number" style="color: var(--text-secondary);"><%= productos.filter(p=>!p.activo).length %></span>
                <span class="stat-label">Inactivos</span>
            </div>
        </div>

        <div class="table-header">
            <h2 class="table-title">INVENTARIO</h2>
            
            <div style="display: flex; gap: 15px; align-items: center;">
                <form action="/admin/dashboard" method="GET" style="display: flex; align-items: center;">
                    <input type="number" name="id" placeholder="Buscar ID..." style="padding: 8px; border: 1px solid #ccc; border-radius: 4px 0 0 4px; width: 100px;">
                    <button type="submit" style="padding: 8px 12px; background: #333; color: white; border: none; border-radius: 0 4px 4px 0; cursor: pointer;">
                        <i class="fas fa-search"></i>
                    </button>
                    <% if (busqueda) { %>
                        <a href="/admin/dashboard" style="margin-left: 10px; color: #ff4842; text-decoration: none; font-size: 0.8rem;">✕ Limpiar</a>
                    <% } %>
                </form>

                <a href="/admin/agregar" class="btn-add"><i class="fas fa-plus"></i> Nuevo</a>
            </div>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Imagen</th>
                        <th>Producto</th>
                        <th>Precio</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (productos.length === 0) { %>
                        <tr>
                            <td colspan="6" style="text-align: center; padding: 30px;">No se encontraron productos.</td>
                        </tr>
                    <% } else { %>
                        <% productos.forEach(p => { %>
                            <tr>
                                <td style="color: #888; font-size: 0.8rem;">#<%= p.id %></td>
                                <td>
                                    <% let src = (p.imagen && p.imagen.startsWith('http')) ? p.imagen : '/uploads/' + p.imagen; %>
                                    <img src="<%= src %>" class="prod-img">
                                </td>
                                <td><strong><%= p.nombre %></strong></td>
                                <td>$<%= p.precio %></td>
                                <td>
                                    <span class="status-badge <%= p.activo ? 'status-active' : 'status-inactive' %>">
                                        <%= p.activo ? 'Activo' : 'Inactivo' %>
                                    </span>
                                </td>
                                <td style="white-space: nowrap;">
                                    <a href="/admin/editar/<%= p.id %>" class="action-btn" title="Editar"><i class="fas fa-pencil-alt"></i></a>
                                    
                                    <a href="/admin/estado/<%= p.id %>" class="action-btn <%= p.activo ? 'btn-toggle' : 'btn-reactivate' %>" title="Cambiar Estado">
                                        <i class="fas <%= p.activo ? 'fa-power-off' : 'fa-check' %>"></i>
                                    </a>

                                    <a href="/admin/eliminar/<%= p.id %>" 
                                       class="action-btn" 
                                       style="color: #ff4842;" 
                                       title="Eliminar Definitivamente"
                                       onclick="return confirm('¿Estás seguro de BORRAR este producto para siempre? No se puede deshacer.')">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

    </div>
    <footer class="main-footer">
        <p>© 2025 TP PROGRAMACIÓN UTN</p>
        <p >Alumnos: Mateo Gigy Traynor - Tomás Santiago Munilla </p>
    </footer>

</body>
</html>