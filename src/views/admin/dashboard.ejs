<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | The Jersey Lab ADMIN</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@500;700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/admin-style.css">
</head>
<body>

    <div class="dashboard-container">
        <header class="admin-header">
            <div class="logo">The Jersey Lab <span>ADMIN</span></div>
            <nav class="admin-actions">
                <span>Hola, Admin</span>
                <a href="/admin/ventas/excel" title="Descargar Ventas"><i class="fas fa-file-csv"></i> Excel</a>
                <a href="/admin/logout" class="btn-logout"><i class="fas fa-sign-out-alt"></i> Salir</a>
            </nav>
        </header>

        <div class="stats-grid">
            <div class="stat-card">
                <span class="stat-number"><%= productos.length %></span>
                <span class="stat-label">Productos Totales</span>
            </div>
            <div class="stat-card">
                <span class="stat-number" style="color: var(--success);">
                    <%= productos.filter(p => p.activo).length %>
                </span>
                <span class="stat-label">Activos</span>
            </div>
            <div class="stat-card">
                <span class="stat-number" style="color: #666;">
                    <%= productos.filter(p => !p.activo).length %>
                </span>
                <span class="stat-label">Inactivos</span>
            </div>
        </div>

        <div class="table-header">
            <h2 class="table-title">INVENTARIO</h2>
            <a href="/admin/agregar" class="btn-add"><i class="fas fa-plus"></i> Nuevo Producto</a>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Img</th>
                        <th>Producto</th>
                        <th>Precio (USD)</th>
                        <th>Categoría</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (productos.length === 0) { %>
                        <tr>
                            <td colspan="6" style="text-align: center; padding: 30px;">No hay productos cargados.</td>
                        </tr>
                    <% } else { %>
                        <% productos.forEach(producto => { %>
                            <tr>
                                <td>
                                    <% let src = (producto.imagen && producto.imagen.startsWith('http')) ? producto.imagen : '/uploads/' + producto.imagen; %>
                                    <img src="<%= src %>" class="prod-img" alt="img">
                                </td>
                                <td style="font-weight: 600;"><%= producto.nombre %></td>
                                <td>$<%= producto.precio %></td>
                                <td style="color: #888;"><%= producto.categoria %></td>
                                <td>
                                    <% if (producto.activo) { %>
                                        <span class="status-badge status-active">Activo</span>
                                    <% } else { %>
                                        <span class="status-badge status-inactive">Inactivo</span>
                                    <% } %>
                                </td>
                                <td>
                                    <a href="/admin/editar/<%= producto.id %>" class="action-btn btn-edit" title="Editar">
                                        <i class="fas fa-pencil-alt"></i>
                                    </a>
                                    
                                    <a href="/admin/estado/<%= producto.id %>" 
                                       class="action-btn <%= producto.activo ? 'btn-toggle' : 'btn-reactivate' %>"
                                       title="<%= producto.activo ? 'Desactivar' : 'Activar' %>">
                                        <i class="fas <%= producto.activo ? 'fa-power-off' : 'fa-check' %>"></i>
                                    </a>
                                </td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

    </div>

</body>
</html>